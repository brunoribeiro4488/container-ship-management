package lapr.project.controller;

import lapr.project.data.ConnectionFactory;
import lapr.project.data.DatabaseConnection;
import lapr.project.data.ShipStoreDB;
import lapr.project.model.Company;
import org.junit.jupiter.api.Assertions;
import org.junit.jupiter.api.Test;

import java.io.IOException;
import java.sql.SQLException;
import java.util.logging.Level;
import java.util.logging.Logger;

import static org.junit.jupiter.api.Assertions.*;

class ShipsAvailableControllerTest {
/*
    @Test
    void getShipsAvailable() throws SQLException, IOException {
        Company company = new Company();
        ShipsAvailableController shipsAvailableController = new ShipsAvailableController(company);
        boolean expected = true;
        boolean result = shipsAvailableController.getShipsAvailable();
        Assertions.assertEquals(expected,result);
    }

 */
}